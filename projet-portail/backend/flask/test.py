# mydict = {'george': 16, 'amber': 19}

# import tensorflow as tf
# import cv2

# img = cv2.imread('uploads/rare diamond.jpg')
# resized = cv2.resize(img, (331, 331))
#expanded = tf.expand_dims(resized, axis=0)

# print(expanded.shape)